<UserControl x:Class="QuestEditor.Explorer.ExplorerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuestEditor.Explorer"
             xmlns:nodes="clr-namespace:QuestEditor.Nodes"
             xmlns:shared="clr-namespace:QuestEditor.Shared"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <local:ExplorerVM/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/QuestEditor;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <HierarchicalDataTemplate DataType="{x:Type local:QuestPackVM}" ItemsSource="{Binding Quests}">
                <local:SelectableTreeViewItem Header="{Binding PackName}" Style="{StaticResource QuestPackItemStyle}"/>
            </HierarchicalDataTemplate>

            <HierarchicalDataTemplate DataType="{x:Type local:QuestVM}" ItemsSource="{Binding Nodes}">
                <local:SelectableTreeViewItem Header="{Binding QuestTag}" Style="{StaticResource QuestItemStyle}"/>
            </HierarchicalDataTemplate>

            <HierarchicalDataTemplate DataType="{x:Type nodes:NodeVM}">
                <local:SelectableTreeViewItem Header="{Binding NodeType}" Style="{StaticResource NodeItemStyle}"/>
            </HierarchicalDataTemplate>

        </ResourceDictionary>
    </UserControl.Resources>

    <TreeView ItemsSource="{Binding QuestPacks}"
              Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
</UserControl>
