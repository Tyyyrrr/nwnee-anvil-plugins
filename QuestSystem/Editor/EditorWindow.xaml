<Window x:Class="QuestEditor.EditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuestEditor"
        xmlns:explorer="clr-namespace:QuestEditor.QuestPackExplorer"
        xmlns:canvas="clr-namespace:QuestEditor.QuestCanvas"
        xmlns:propList="clr-namespace:QuestEditor.PropertyList"
        mc:Ignorable="d"
        Title="Quest Editor" Height="450" Width="800"
        Background="LightGray">

    <Grid Background="LightGray" Margin="0,0,8,8">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <explorer:QuestPackExplorerView x:Name="ExplorerView" Grid.Column="0"/>

        <canvas:QuestCanvasView x:Name="CanvasView" Grid.Column="1" 
            AddStageCommand="{Binding AddStageCommand}"
            RemoveStageCommand="{Binding RemoveStageCommand}"
            OverlayCapturesInput="{Binding OverlayCapturesInput, Mode=TwoWay}" />

    </Grid>

</Window>
